## db create drop/tables
export DEV_DATABASE_URL="sqlite:///flask_app.db" 
export FLASK_APP=app:create_app
flask shell
db.create_all()
db.drop_all()

## Add user from shell ##
from app.models import User
user = User(name="AAA", last_name="aaa", password="password1", email="email1@testemail.com", role_id=1)
user = User(name="BBB", last_name="bbb", password="password2", email="email2@testemail.com", role_id=1)
db.session.add(user)
db.session.commit()

## Add role from shell ##
from app.models import Role
role=Role(name="admin")
role=Role(name="customer")
db.session.add(role)
db.session.commit()

## Add park from shell ##
from app.models import Park
park= Park(name="PARK 1", location="Dublin", description="AAAAAAAAAAAAAAAA aaaaaaaaaaaaaaaaaa")
park= Park(name="PARK 2", location="London", description="BBBBBBBBBBBBBBBB bbbbbbbbbbbbbbbbbb")
db.session.add(park)
db.session.commit()

## Run app locally ##
flask --app app run
flask run